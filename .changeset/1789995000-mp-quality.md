---
mp_core:
  type: fix
  version: "0.1.0"
mp_genai:
  type: fix
  version: "0.1.0"
mp_text:
  type: fix
  version: "0.1.0"
mp_audio:
  type: docs
  version: "0.1.0"
mp_camera:
  type: docs
  version: "0.1.0"
mp_vision:
  type: docs
  version: "0.1.0"
---

# Fix task-lifecycle deadlocks and clarify a missing native runtime

Fixes three defects that could strand a task or surface an unusable error:

- The `mp_text` and `mp_genai` platform bridges set their busy flag before
  invoking an operation, so an operation that threw before returning a future
  left the task permanently busy. Rejecting a non-8-bit image on an Android LLM
  session was the reachable case; every later call then failed with
  `failedPrecondition`.
- `LlmInference.generateResponse` attached its session cleanup to a derived
  future, so a caller that handled a generation error still received an
  unhandled zone error, and a synchronous failure leaked the temporary session.
- A build with no linked MediaPipe runtime failed inside `dart:ffi` with a bare
  `ArgumentError` naming an internal asset id. Native task creation now reports
  `MpStatus.unavailable` with the user define that supplies a local runtime.

Package READMEs are now generated from shared `mdt` providers, so the install
steps, lifecycle contract, model contract, and failure contract stay identical
across every package and the documentation site.
